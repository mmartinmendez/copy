import React from 'react'

const SplitTitle = ({title}) => (
    <div className="split-container">
        <div className="title-divider"></div>
        <p className="split-title">{title}</p>
    </div>
)

export default SplitTitle